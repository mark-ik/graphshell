// Slice 66: body moved to register-lens. Re-export shim.
pub(crate) use register_lens::{
    GlyphAnchor, GlyphOverlay, LensOverlayDescriptor, LensRegistry, LENS_ID_DEFAULT,
    LENS_ID_SEMANTIC_OVERLAY,
};
