[package]
name = "register-layout"
version = "0.0.1"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"

[lib]
path = "src/lib.rs"
