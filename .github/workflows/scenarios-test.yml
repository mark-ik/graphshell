name: Scenarios Test Binary

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scenarios:
    name: scenarios (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            allow_failure: false
          - os: windows-latest
            allow_failure: false
          - os: macos-latest
            allow_failure: true

    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.allow_failure }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev pkg-config

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Run scenarios binary
        if: runner.os == 'Linux'
        run: pwsh -NoProfile -File scripts/dev/test-select.ps1 run-policy --tier pr-optional --platform linux --quiet

      - name: Run scenarios binary (Windows)
        if: runner.os == 'Windows'
        run: pwsh -NoProfile -File scripts/dev/test-select.ps1 run-policy --tier pr-optional --platform windows --quiet

      - name: Run scenarios binary (macOS)
        if: runner.os == 'macOS'
        run: pwsh -NoProfile -File scripts/dev/test-select.ps1 run-policy --tier pr-optional --platform macos --quiet
